---
grafana_package: "grafana{{ (grafana_version != 'latest') | ternary('=' ~ grafana_version, '') }}"
grafana_dependencies:
  - apt-transport-https
  - adduser
  - ca-certificates
  - libfontconfig
  - gnupg2
